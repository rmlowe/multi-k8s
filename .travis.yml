sudo: required
services:
  - docker

before_install:
  - docker build -t rmlowe/react-test -f ./client/Dockerfile.dev ./client

script:
  - docker run rmlowe/react-test npm test -- --coverage

after_success:
  - docker build -t rmlowe/multi-client ./client
  - docker build -t rmlowe/multi-nginx ./nginx
  - docker build -t rmlowe/multi-server ./server
  - docker build -t rmlowe/multi-worker ./worker
  # Log in to the docker CLI
  - echo "$DOCKER_PASSWORD" | docker login -u "$DOCKER_ID" --password-stdin
  # Take those images and push them to docker hub
  - docker push rmlowe/multi-client
  - docker push rmlowe/multi-nginx
  - docker push rmlowe/multi-server
  - docker push rmlowe/multi-worker

deploy:
  provider: elasticbeanstalk
  region: us-west-1
  app: multi-docker
  env: MultiDocker-env
  bucket_name: elasticbeanstalk-us-west-1-498000146110
  bucket_path: docker-multi
  on:
    branch: master
  access_key_id: $AWS_ACCESS_KEY
  secret_access_key:
    secure: $AWS_SECRET_KEY
